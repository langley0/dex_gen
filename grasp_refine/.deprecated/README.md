Legacy `grasp_refine` modules were moved into `.deprecated/legacy_v1/` to make
room for a clean rebuild.

Current rebuild status:

- root package is being reconstructed from the data layer upward
- first new step is DGA-style source artifact conversion
- model, diffusion, and training modules will be added incrementally
